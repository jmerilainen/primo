name: Tests

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    ssteps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - run: yarn install
      - run: yarn lint
      - run: yarn test
